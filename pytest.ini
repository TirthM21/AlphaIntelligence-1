[pytest]
testpaths = tests
markers =
    network: tests that require external network/API access
    slow: tests that are computationally expensive or long running
    integration: multi-component integration tests
addopts = -m "not network"

